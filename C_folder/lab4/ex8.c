/*Modify the programs in questions 5 and 6, so that they use structs for storing
students information. Each write or read should write/read one struct. Note: you
have to make use of sizeof() to obtain the required number of bytes to write/read.*/
